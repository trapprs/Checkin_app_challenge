
cache:
- bundler

sudo: false
branches:
only:
- develop
script:
- xcodebuild [DEFAULT_OPTIONS] CODE_SIGNING_REQUIRED=NO

language: objective-c
podfile: Podfile

xcode_project: CheckinAppChallenge # path to your xcodeproj folder
xcode_scheme: CheckinAppChallengeTests
xcode_destination: platform=iOS Simulator,OS=11.3,name=iPhone X
